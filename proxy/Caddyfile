http://hestia.aet.cit.tum.de {
    redir https://{host}{uri}
}

https://hestia.aet.cit.tum.de {
    tls /cert.pem /key.pem

    handle /api/* {
        reverse_proxy server:8080
    }

    handle {
        respond "tbd"
    }
}